"""C5 — immune system: defence against memory poisoning.

Validates provenance at ingest, detects semantic anomalies and injection
patterns, and quarantines instead of deleting so the rejection stays auditable.

Placeholder: implemented in Phase 2 (the project plan §10). Gated by
``AletheiaConfig.enable_immune``.
"""
