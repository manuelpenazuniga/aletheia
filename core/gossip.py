"""C4 — gossip: propagation of findings between agents.

Information degrades as it propagates (``hop_count``) — the adverse phenomenon
that motivates consolidation, which repairs it.

Placeholder: implemented in Phase 2 (CLAUDE.md §10). Gated by
``AletheiaConfig.enable_gossip``.
"""
