"""C1 — fast episodic write + budgeted semantic retrieval.

Placeholder: implemented in Phase 1 (CLAUDE.md §10) on top of
:class:`core.adapter.StorageAdapter`. The module exists from Phase 0 so the
repository layout matches CLAUDE.md §4 exactly.
"""
